// Nous Portal inference gateway. Catalog is account-scoped live `/v1/models`
// (dashboard key). Deliberately NO `discovery` node so generate-models.ts does
// not freeze one account's roster into models.json.
provider "nous-portal" {
	default-model "anthropic/claude-sonnet-4.6"
	env "NOUS_API_KEY"
	dynamic-models-authoritative #true
	// Live `/v1/models` is the whole truth per account. Same-id rows on other
	// hosts disagree on tariffs; never borrow pricing. Canonical limits and
	// reasoning flags are recovered at runtime by nousPortalModelManagerOptions.
}
