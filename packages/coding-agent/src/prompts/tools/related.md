Walk typed neighbors of one native Mnemon insight.

Use after `recall` or `retain` returns an id and you need the graph around it — cause, correction, or same-entity rows that flat similarity misses.

Required: `id`. Optional `type` (`causal` · `semantic` · `temporal` · `entity` · `supersedes`) and `depth` (1–4, default 2).
