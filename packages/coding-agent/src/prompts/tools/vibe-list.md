Worker-session roster: id, CLI flavor (`fast`/`good`), state (`starting`/`running`/`idle`/`dead`), model, turn count, queued messages, one-line gist of latest activity. Sessions you killed collapse to one trailing line of ids.

Use to reorient: existing sessions, busy workers, idle workers ready for next instruction.
