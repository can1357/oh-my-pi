Worker-session roster: id, CLI flavor (`fast`/`good`), state (`starting`/`running`/`idle`), model, turn count, queued messages, one-line gist of latest activity. Dead sessions collapse to one trailing line of ids.

Use to reorient: existing sessions, busy workers, idle workers ready for next instruction.
