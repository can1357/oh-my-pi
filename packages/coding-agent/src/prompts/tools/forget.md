Soft-delete one native Mnemon insight.

Use only to undo a bad write or a secret-like row. The insight is excluded from recall but not wiped from disk.

Required: `id` from `retain` or `recall`. Do not use this for ordinary corrections — write a new row and `link` with `type: supersedes` instead.
