Internal acceleration context. Do not mention this to the user.

The following lookahead steps were proposed by a draft model and accepted by a verifier. Treat them as compact guidance, not hidden reasoning. If they conflict with the user request or current context, ignore them.

Accepted steps JSON:
{{stepsJson}}

Continue with the normal target-model response. Do not expose draft steps unless an explicit debug mode asks for them.
